#!/usr/bin/R
 #  statistical with R 
#  use the question mark to get information about the data set 
# Get information
data_cars <-  mtcars

dim(data_cars)

# find the varibles
names(data_cars)

# get the rownames
rownames(data_cars)

# get the columns 
columnnames(data_cars)
